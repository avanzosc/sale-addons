<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="sale_requisition_type_view_tree" model="ir.ui.view">
        <field name="model">sale.requisition.type</field>
        <field name="arch" type="xml">
            <tree string="Sale Agreement Types">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="exclusive"/>
            </tree>
        </field>
    </record>

    <record id="sale_requisition_type_view_kanban" model="ir.ui.view">
        <field name="model">sale.requisition.type</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="name"/>
                <field name="exclusive"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_global_click">
                            <div class="o_kanban_record_top ">
                                <div class="o_kanban_record_headings mt4">
                                    <strong class="o_kanban_record_title"><field name="name"/></strong>
                                </div>
                                <field name="exclusive" widget="label_selection"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="sale_requisition_type_view_form" model="ir.ui.view">
        <field name="model">sale.requisition.type</field>
        <field name="arch" type="xml">
            <form string="Sale Agreement Types">
                <sheet>
                    <group>
                        <group string="Agreement Type">
                            <field name="name"/>
                            <field name="exclusive" widget="radio"/>
                        </group>
                        <group string="Data for new quotations">
                            <field name="line_copy" widget="radio"/>
                            <field name="quantity_copy" widget="radio"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="sale_requisition_type_action" model="ir.actions.act_window">
        <field name="name">Sale Agreement Types</field>
        <field name="res_model">sale.requisition.type</field>
        <field name="context">{}</field>
        <field name="view_mode">tree,kanban,form</field>
    </record>

    <menuitem id="menu_sale_requisition_type"
              sequence="2"
              parent="sale.menu_sale_config"
              action="sale_requisition_type_action"/>
</odoo>
