<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
  <record id="view_move_form" model="ir.ui.view">
    <field name="model">stock.move</field>
    <field name="inherit_id" ref="stock.view_move_form" />
    <field name="arch" type="xml">
      <field name="origin" position="after">
        <field name="picking_type_code" invisible="1" />
        <field name="sale_type_id" invisible="picking_type_code != 'outgoing'" />
      </field>
    </field>
  </record>

  <record id="view_move_tree" model="ir.ui.view">
    <field name="model">stock.move</field>
    <field name="inherit_id" ref="stock.view_move_tree" />
    <field name="arch" type="xml">
      <field name="reference" position="after">
        <field name="sale_type_id" optional="show" />
      </field>
    </field>
  </record>

  <record id="view_move_search" model="ir.ui.view">
    <field name="model">stock.move</field>
    <field name="inherit_id" ref="stock.view_move_search" />
    <field name="arch" type="xml">
      <field name="partner_id" position="after">
        <field name="sale_type_id" />
      </field>
      <filter name="groupby_picking_type_id" position="after">
        <filter
          string="Sale type"
          name="groupby-sale-type"
          domain="[]"
          context="{'group_by': 'sale_type_id'}"
        />
      </filter>
    </field>
  </record>
</odoo>
