<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_order_form" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_blanket_order.view_order_form" />
        <field name="arch" type="xml">
            <field name="blanket_order_id" position="attributes">
                <attribute name="invisible">0</attribute>
                <attribute name="readonly">1</attribute>
            </field>
        </field>
    </record>

    <record id="view_order_tree" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field name="blanket_order_id" optional="show" readonly="1" />
            </field>
        </field>
    </record>

    <record id="view_quotation_tree" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field name="blanket_order_id" optional="show" readonly="1" />
            </field>
        </field>
    </record>

    <record id="view_sales_order_filter" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <field name="team_id" position="after">
                <field name="blanket_order_id" />
            </field>
            <filter name="customer" position="after">
                <filter
          name="by_blanket_order"
          string="Blanket Order"
          domain="[]"
          context="{'group_by': 'blanket_order_id'}"
        />
            </filter>
        </field>
    </record>
</odoo>
